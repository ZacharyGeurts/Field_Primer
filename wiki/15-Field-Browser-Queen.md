# 15 — Field Browser Queen

**Nothing optional. Hold all gates. MP4. We want it ALL.**

## Doctrine

Reality is 3D · Time is linear · Energy can be moved.

The Queen browser carries the **full web** into the packet field. We do not amputate capabilities to feel safe. We **hold gates**:

| Capability | Queen posture |
|------------|---------------|
| WebGL / WebGPU | On — thermo per context |
| WebRTC | On — gatekeeper per peer |
| MSE / MP4 / H.264 / AAC | **Mandatory in-tree** |
| EME / Widevine | On — **held**, not omitted |
| Service Workers / WASM | On — entropy receipts |
| Geolocation / camera / mic | On — operator consent gate |
| Legacy plugins | WASM surrogates — surface preserved |

**Wrong:** "Disable WebRTC for privacy."  
**Right:** WebRTC flows through Connection Gatekeeper with honorability + packet field.

## FieldFox (ship now)

Hardened Gecko fork:

- Telemetry stripped
- Truth Resolver DNS lock (`127.0.0.1`)
- NEXUS native messaging bridge
- Full codec tree — MP4 is not an optional pack

```bash
NEXUS_FIELD_BROWSER_QUEEN=1 ./lib/fieldfox-launch.sh
python3 lib/field-queen-browser.py json   # queen_verdict: QUEEN_READY
```

## NEXUS stack binding

```
Navigation → Packet Field → Gatekeeper → Honorability → Thermo receipt
                ↑
         Truth DNS (no Google shortcut)
                ↑
         Sovereign Time (SQUIDGIE witness)
```

## Millennium track

Ladybird / Servo — same gate doctrine, no Chromium dependency. FieldFox ships first; millennium engine inherits `field-queen-gates-seed.json`.

## Panel

Threat panel merges `field_queen_browser` slice:

- `gates.all_held` — every gate held
- `codecs.mp4_mandatory` — true
- `queen_verdict` — `QUEEN_READY`
- `browser_awareness` — live tab honorability

## ELLIE alignment

Captain Ellie seals time forward. Queen seals **egress** forward — every capability exists; every wire exit earns a receipt.

See [NEXUS-Shield FIELD-BROWSER-2026](https://github.com/ZacharyGeurts/NEXUS-Shield/blob/main/FIELD-BROWSER-2026.md).